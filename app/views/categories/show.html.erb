<h1><%= @category.title %></h1>
<ul>
<% @category.items.each do |item| %>
    <li><%= link_to item.title + " $#{item.price.to_f/100}", item_path(item) %></li>
    <%= button_to 'Add to Cart', line_items_path(item_id: item), method: 'post' %>
<% end %>
</ul>